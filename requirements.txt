# ── Core ─────────────────────────────────────────
streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
pyyaml>=6.0.1

# ── Collectors ───────────────────────────────────
google-play-scraper>=1.2.4
pytrends>=4.9.2
requests>=2.31.0
tenacity>=8.2.3
beautifulsoup4>=4.12.0
lxml>=4.9.3
geopy>=2.4.1

# ── Social (optional — graceful fallback) ────────
telethon>=1.34.0; python_version >= "3.8"

# ── NLP ──────────────────────────────────────────
transformers>=4.38.0
torch>=2.2.0; sys_platform != "darwin" or platform_machine != "arm64"
torch>=2.2.0; sys_platform == "darwin" and platform_machine == "arm64"
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0

# ── Models ───────────────────────────────────────
scikit-learn>=1.4.0
prophet>=1.1.5

# ── Database ─────────────────────────────────────
sqlalchemy>=2.0.0

# ── Map ──────────────────────────────────────────
folium>=0.15.1
streamlit-folium>=0.18.0

# ── Utilities ────────────────────────────────────
python-dotenv>=1.0.0
nest-asyncio>=1.6.0
tqdm>=4.66.0
